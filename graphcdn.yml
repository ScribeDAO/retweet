name: scribedao
schema: './schema.graphql'
originUrl: 'https://retweet.scribedao.com/api/graphql'
rules:
  - types:
      - Query
    maxAge: 900
    swr: 900
    description: Cache everything (default)
keyFields:
  types: {}
